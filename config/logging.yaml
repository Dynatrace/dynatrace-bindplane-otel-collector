# File logging configuration assumes DBDOT_OTEL_COLLECTOR_HOME
# is set in the agent's environment. 
output: file
level: info
file:
  filename: "${DBDOT_OTEL_COLLECTOR_HOME}/log/collector.log"
  maxbackups: 5
  maxsize: 100
  maxage: 7
