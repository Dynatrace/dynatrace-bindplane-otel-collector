version: 2

project_name: dbdot-collector

before:
  hooks:
    - make release-prep CURR_VERSION={{ .Version }}

# https://goreleaser.com/customization/build/
# https://goreleaser.com/customization/builds/#import-pre-built-binaries
builds:
  - id: collector
    builder: prebuilt
    binary: dbdot-collector
    goos:
      - linux
    goarch:
      - amd64
      - arm64
      - ppc64le
    prebuilt:
      path: tmp/collector_{{ .Os }}_{{ .Arch }}

# Build container images with docker buildx (mutli arch builds).
# A single scratch-based image per arch: collector binary, passwd/group,
# CA certificates, and the /etc/otel runtime tree only.
dockers:
  - id: scratch-amd64
    goos: linux
    goarch: amd64
    retry:
      attempts: 10
      delay: 30s
      max_delay: 1m
    ids:
      - collector
    image_templates:
      - "observiq/dbdot-collector-amd64:latest"
      - "observiq/dbdot-collector-amd64:{{ .Version }}"
      - "ghcr.io/dynatrace/dbdot-collector-amd64:latest"
      - "ghcr.io/dynatrace/dbdot-collector-amd64:{{ .Version }}"
      - "us-central1-docker.pkg.dev/{{ .Env.GAR_PROJECT_ID }}/collector/dbdot-collector-amd64:latest"
      - "us-central1-docker.pkg.dev/{{ .Env.GAR_PROJECT_ID }}/collector/dbdot-collector-amd64:{{ .Version }}"
    dockerfile: ./docker/Dockerfile.scratch
    use: buildx
    build_flag_templates:
      - "--label=created={{.Date}}"
      - "--label=title={{.ProjectName}}"
      - "--label=revision={{.FullCommit}}"
      - "--label=version={{.Version}}"
      - "--platform=linux/amd64"
    extra_files:
      - release_deps/plugins
      - release_deps/config.yaml
      - config/logging.stdout.yaml
  - id: scratch-arm64
    goos: linux
    goarch: arm64
    retry:
      attempts: 10
      delay: 30s
      max_delay: 1m
    ids:
      - collector
    image_templates:
      - "observiq/dbdot-collector-arm64:latest"
      - "observiq/dbdot-collector-arm64:{{ .Version }}"
      - "ghcr.io/dynatrace/dbdot-collector-arm64:latest"
      - "ghcr.io/dynatrace/dbdot-collector-arm64:{{ .Version }}"
      - "us-central1-docker.pkg.dev/{{ .Env.GAR_PROJECT_ID }}/collector/dbdot-collector-arm64:latest"
      - "us-central1-docker.pkg.dev/{{ .Env.GAR_PROJECT_ID }}/collector/dbdot-collector-arm64:{{ .Version }}"
    dockerfile: ./docker/Dockerfile.scratch
    use: buildx
    build_flag_templates:
      - "--label=created={{.Date}}"
      - "--label=title={{.ProjectName}}"
      - "--label=revision={{.FullCommit}}"
      - "--label=version={{.Version}}"
      - "--platform=linux/arm64"
    extra_files:
      - release_deps/plugins
      - release_deps/config.yaml
      - config/logging.stdout.yaml
  - id: scratch-ppc64le
    goos: linux
    goarch: ppc64le
    retry:
      attempts: 10
      delay: 30s
      max_delay: 1m
    ids:
      - collector
    image_templates:
      - "observiq/dbdot-collector-ppc64le:latest"
      - "observiq/dbdot-collector-ppc64le:{{ .Version }}"
      - "ghcr.io/dynatrace/dbdot-collector-ppc64le:latest"
      - "ghcr.io/dynatrace/dbdot-collector-ppc64le:{{ .Version }}"
      - "us-central1-docker.pkg.dev/{{ .Env.GAR_PROJECT_ID }}/collector/dbdot-collector-ppc64le:latest"
      - "us-central1-docker.pkg.dev/{{ .Env.GAR_PROJECT_ID }}/collector/dbdot-collector-ppc64le:{{ .Version }}"
    dockerfile: ./docker/Dockerfile.scratch
    use: buildx
    build_flag_templates:
      - "--label=created={{.Date}}"
      - "--label=title={{.ProjectName}}"
      - "--label=revision={{.FullCommit}}"
      - "--label=version={{.Version}}"
      - "--platform=linux/ppc64le"
    extra_files:
      - release_deps/plugins
      - release_deps/config.yaml
      - config/logging.stdout.yaml

docker_manifests:
  - name_template: "observiq/dbdot-collector:latest"
    image_templates:
      - "observiq/dbdot-collector-amd64:latest"
      - "observiq/dbdot-collector-arm64:latest"
      - "observiq/dbdot-collector-ppc64le:latest"
    skip_push: false
  - name_template: "observiq/dbdot-collector:{{ .Version }}"
    image_templates:
      - "observiq/dbdot-collector-amd64:{{ .Version }}"
      - "observiq/dbdot-collector-arm64:{{ .Version }}"
      - "observiq/dbdot-collector-ppc64le:{{ .Version }}"
    skip_push: false
  - name_template: "ghcr.io/dynatrace/dbdot-collector:latest"
    image_templates:
      - "ghcr.io/dynatrace/dbdot-collector-amd64:latest"
      - "ghcr.io/dynatrace/dbdot-collector-arm64:latest"
      - "ghcr.io/dynatrace/dbdot-collector-ppc64le:latest"
    skip_push: false
  - name_template: "ghcr.io/dynatrace/dbdot-collector:{{ .Version }}"
    image_templates:
      - "ghcr.io/dynatrace/dbdot-collector-amd64:{{ .Version }}"
      - "ghcr.io/dynatrace/dbdot-collector-arm64:{{ .Version }}"
      - "ghcr.io/dynatrace/dbdot-collector-ppc64le:{{ .Version }}"
    skip_push: false
  - name_template: "us-central1-docker.pkg.dev/{{ .Env.GAR_PROJECT_ID }}/collector/dbdot-collector:latest"
    image_templates:
      - "us-central1-docker.pkg.dev/{{ .Env.GAR_PROJECT_ID }}/collector/dbdot-collector-amd64:latest"
      - "us-central1-docker.pkg.dev/{{ .Env.GAR_PROJECT_ID }}/collector/dbdot-collector-arm64:latest"
      - "us-central1-docker.pkg.dev/{{ .Env.GAR_PROJECT_ID }}/collector/dbdot-collector-ppc64le:latest"
    skip_push: false
  - name_template: "us-central1-docker.pkg.dev/{{ .Env.GAR_PROJECT_ID }}/collector/dbdot-collector:{{ .Version }}"
    image_templates:
      - "us-central1-docker.pkg.dev/{{ .Env.GAR_PROJECT_ID }}/collector/dbdot-collector-amd64:{{ .Version }}"
      - "us-central1-docker.pkg.dev/{{ .Env.GAR_PROJECT_ID }}/collector/dbdot-collector-arm64:{{ .Version }}"
      - "us-central1-docker.pkg.dev/{{ .Env.GAR_PROJECT_ID }}/collector/dbdot-collector-ppc64le:{{ .Version }}"
    skip_push: false
changelog:
  # Set this to true if you don't want any changelog at all.
  #
  # Warning: this will also ignore any changelog files passed via `--release-notes`,
  # and will render an empty changelog.
  #
  # This may result in an empty release notes on GitHub/GitLab/Gitea.
  #
  # Templates: allowed.
  disable: true
